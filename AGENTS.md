# contention

Content intelligence tool: a hybrid-RAG Content Strategist and an ML-driven Content Optimiser. See `README.md`.

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues on Jarrod-Bob/contention, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage labels are used as-is: needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
